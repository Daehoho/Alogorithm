#include <cstdio>
#include <cstring>
#include <stack>

/* how to use stack */
/*stack<int> st;
st.push(1);
st.pop();
st.empty();*/

int main (void) {
    int num;
    int cows[80001];
    scanf("%d", &num);

    for(int i = 0; i < num; i++ ) {
    }
    return 0;
}
