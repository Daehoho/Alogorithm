#include <cstdio>
#include <vector>

using namespace std;

int V[501];
int E[6001];
int C[10001];

int main (void) {
    int N, M;
    int s, e, c;
    scanf("%d %d", &N, &M);

    for(int i = 0; i < M; i++) {
        scanf("%d %d %d", );
    }

    return 0;
}
